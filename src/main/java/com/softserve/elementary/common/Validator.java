package com.softserve.elementary.common;

public class Validator {
/*
    public static boolean checkTheLengthOfParams(String[] args, int len) {
        if (args.length != len) {
            System.out.println("Incorrect number of input parameters. " +
                    "It should be " + len + " parameters.");
            return false;
        }
        return true;
    }


    public static boolean checkIfPositiveNumber(double... numbers) {
        for (double number : numbers) {
            if (number <= 0) {
                System.out.println("All arguments should be a positive.");
                return false;
            }
        }
        return true;
    }

    public static boolean isValidEnvelopeSide(double[] parameters) {
        if (parameters.length != 4) {
            System.out.println("Incorrect number of input parameters. " +
                    "It should be inserted 4 sides.");
            return false;
        }
        if (!checkIfPositiveNumber(parameters)) {
            return false;
        }
        return true;
    }
*/

}
